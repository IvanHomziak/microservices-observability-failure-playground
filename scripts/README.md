# Incident Trigger Scripts

These scripts are lightweight entry points for reproducible incident generation.

- `trigger-payment-timeout.sh` - placeholder trigger workflow for scenario 001.

Additional scripts should map 1:1 with scenario playbooks in `scenarios/`.
