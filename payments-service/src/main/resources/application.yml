spring:
  application:
    name: payments-service
  kafka:
    bootstrap-servers: localhost:9092

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

failure-simulation:
  payments:
    delay-ms: 5000
    forced-status-code: 0
    failure-rate: 0.0
    invalid-json-enabled: false
    decline-enabled: false
    timeout-enabled: false
