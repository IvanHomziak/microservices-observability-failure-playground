spring:
  application:
    name: api-gateway

services:
  orders:
    base-url: http://localhost:8081

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus,metrics
